import RegisterForm from '../components/RegisterForm';

export default function Register() {
  return (
    <section className="register-section flex justify-center">
      <RegisterForm />
    </section>
  );
}
